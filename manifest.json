{
  "schemaVersion": 1,
  "id": "dz0ny.nix-apps",
  "name": "Nix Apps",
  "version": "1.1.0",
  "author": "dz0ny",
  "license": "MIT",
  "description": "Install big desktop apps from nixpkgs with Determinate Nix, for architectures (aarch64) where Arch has no package.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "NixApps.qml"
  },
  "barWidget": {
    "displayName": "Nix Apps",
    "description": "Appears only when installed Nix apps have newer builds waiting. Click to upgrade, middle-click to re-check, right-click for the list.",
    "category": "System",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "checkIntervalSec": 3600
    },
    "schema": [
      {
        "key": "checkIntervalSec",
        "type": "integer",
        "label": "Check interval (seconds)",
        "min": 300,
        "max": 86400,
        "step": 300,
        "defaultValue": 3600
      }
    ]
  }
}
